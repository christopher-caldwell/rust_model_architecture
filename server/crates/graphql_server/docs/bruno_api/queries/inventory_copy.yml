info:
  name: Inventory Copy
  type: graphql
  seq: 3

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |-
      query InventoryCopy($barcode: String!) {
        inventoryCopy(barcode: $barcode) {
          barcode
          createdAt
          updatedAt
          authorName
          status
        }
      }
    variables: |-
      {
        "barcode": "BC-9780132350884-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
