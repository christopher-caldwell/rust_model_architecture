info:
  name: Get List
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/contact-inquiries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
