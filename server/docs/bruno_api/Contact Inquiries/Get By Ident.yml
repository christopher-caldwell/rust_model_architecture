info:
  name: Get By Ident
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/contact-inquiries/{{ident}}"
  auth: inherit

runtime:
  variables:
    - name: ident
      value: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
