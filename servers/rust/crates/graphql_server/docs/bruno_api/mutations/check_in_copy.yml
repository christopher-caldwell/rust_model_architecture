info:
  name: Return Book Copy
  type: graphql
  seq: 17

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |-
      mutation ReturnBookCopy($barcode: String!) {
        returnBookCopy(barcode: $barcode) {
          loanNumber
          createdAt
          updatedAt
          dueAt
          returnedAt
        }
      }
    variables: |-
      {
        "barcode": "BC-9780132350884-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
