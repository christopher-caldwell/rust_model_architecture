info:
  name: Report Lost Loaned Book Copy
  type: graphql
  seq: 18

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |-
      mutation ReportLostLoanedBookCopy($barcode: String!) {
        reportLostLoanedBookCopy(barcode: $barcode) {
          barcode
          createdAt
          updatedAt
          authorName
          status
        }
      }
    variables: |-
      {
        "barcode": "BC-9780132350884-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
