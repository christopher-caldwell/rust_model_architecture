info:
  name: Mark Book Copy Lost
  type: graphql
  seq: 6

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |-
      mutation MarkBookCopyLost($barcode: String!) {
        markBookCopyLost(barcode: $barcode) {
          barcode
          createdAt
          updatedAt
          authorName
          status
        }
      }
    variables: |-
      {
        "barcode": "BC-9780132350884-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
